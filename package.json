{
  "name": "kateandneil-site-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only test tooling for the kateandneil.com static site. Not shipped to the published site.",
  "license": "UNLICENSED",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "fast-check": "^3.23.2",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.9"
  }
}
